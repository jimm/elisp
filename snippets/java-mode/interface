# -*- mode: snippet -*-
# name: public interface XXX { ... }
# key: interface
# --
package `(path-to-java-package (buffer-file-name))`;

public interface ${1:`(substring (file-name-nondirectory (buffer-file-name)) 0 -5)`} {
    $0
}
